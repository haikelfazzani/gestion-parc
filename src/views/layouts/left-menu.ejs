<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">

    <button type="button" id="sidebarCollapse" class="navbar-btn">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <button class="btn btn-dark d-inline-block d-lg-none ml-auto" type="button" data-toggle="collapse"
      data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
      aria-label="Toggle navigation">
      <i class="fas fa-align-justify"></i>      
    </button>

    <% if(locals.userInfo && locals.routesStruct) { %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="nav navbar-nav ml-auto">

        <li class="nav-item active">
          <a class="nav-link" href="/notifications">
            <i class="fas fa-bell"></i>
          </a>
        </li>

        <% if(locals.avatar && locals.staticFiles) { %>
        <li class="nav-item active">
          
            <img src="<%= locals.avatar %>" 
             alt="" id="img-profile"style="border-radius: 50%" width="40" height="40">
          
        </li>
        <%  } %>
          

        <li class="nav-item active">
          <a class="nav-link" href="<%= routesStruct.users.profile %>">
            <%= userInfo.nom_complet %>
          </a>
        </li>

      </ul>
    </div>
    <% } %>
  </div>
</nav>