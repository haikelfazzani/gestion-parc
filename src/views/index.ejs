<% include partials/head %>

<% include partials/menu %>

<div class="container mt-5">

  <% if(locals.userInfo) { %>
  <section class="jumbotron text-center">
    <div class="container">

      <h1 style="font-size: 60px"><i class="fas fa-spa"></i></h1>
      <h1 class="jumbotron-heading">Bienvenue <%= userInfo.nom_complet %></h1>

      <p class="lead text-muted">
        Espace de gestion de reservation des vehicules
      </p>

      <p>
        <a href="/notifications" class="btn btn-primary my-2">
          <i class="fas fa-bell"></i> notifications
        </a>

        <a href="/profile" class="btn btn-secondary my-2">
          <i class="fas fa-user"></i> profile
        </a>
      </p>

    </div>
  </section>
  <% } %>

</div>

<% include partials/footer %>